{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": [
      "/__railway_disabled__/**"
    ]
  },
  "deploy": {
    "healthcheckPath": "/api/healthz",
    "healthcheckTimeout": 300,
    "restartPolicyType": "NEVER",
    "restartPolicyMaxRetries": 0
  }
}
