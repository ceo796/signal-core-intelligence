Clean up the Signal87 AI dashboard so it no longer shows fake/example AI answers, fake sources, fake research outputs, or demo content.

Strict scope:
This is a dashboard cleanup and layout reconciliation only. Do not change authentication, routing, document upload logic, chat logic, brief generation, APIs, database queries, environment variables, Clerk/Supabase/OpenAI configuration, or protected platform functionality.

Current issue:
The dashboard is still too congested and includes fake/example content inside the Signal87 AI panel, including sample answers, fake citations, fake source chips, and demo follow-up prompts. This makes the product feel unprofessional and confusing.

Required changes:

1. Remove all fake/demo content from the dashboard:

* Remove “EXAMPLE ANSWER”
* Remove fake answer text
* Remove fake source chips
* Remove fake suggested follow-up prompts
* Remove any hardcoded demo research response
* Remove any fake citations or fake document references

2. Keep the Signal87 AI panel, but make it clean and functional:
   When there is no active conversation yet, the panel should show a calm empty state such as:
   “Ask Signal87 a question to begin.”
   Subtext:
   “Use general AI, or ground answers in your uploaded documents when sources are available.”

3. Keep only one real input area:

* Main command bar at the top may remain.
* Signal87 AI panel may have a follow-up input only if it is connected to actual chat behavior.
* Do not show fake chat history.

4. Recent work:
   If the unified “Recent work” section has already been implemented, keep it.
   If not, merge Recent documents and Recent briefs into one “Recent work” section.
   Do not use separate cards for Recent documents and Recent briefs.

5. Suggested actions:
   Keep Suggested actions only if they trigger or link to real functionality.
   If they are placeholders, visually mute them or remove them.
   Do not present fake actions as working features.

6. Recent activity:
   Keep Recent activity only if it reflects real activity.
   If no real activity exists, show a quiet empty state:
   “No recent activity yet.”

7. Visual simplification:

* Reduce congestion.
* Use fewer panels.
* Remove fake content.
* Increase breathing room.
* Keep the cream/black visual direction.
* Preserve the agentic, technical sans-serif style.
* Avoid purple as the dominant accent.
* Avoid bubbly card styling.

8. Do not over-edit:
   Do not rewrite unrelated components.
   Do not modify backend files.
   Do not modify API behavior.
   Do not modify auth.
   Do not modify database schema.
   Do not modify upload or brief generation.

Implementation instruction:
First inspect the dashboard component and identify where the hardcoded demo content is coming from. Remove only the fake/demo dashboard content and reconcile the layout around real app state.

Validation:
Verify:

* Dashboard renders cleanly with no fake answers.
* No fake source chips appear.
* No fake citations appear.
* No demo prompts appear unless they are clearly lightweight suggestions and not presented as actual prior conversation.
* Upload document entry point still appears.
* Create brief entry point still appears.
* Signal87 AI input still appears.
* Mobile layout still stacks cleanly.
* No console errors.
* No horizontal overflow.
* No backend/auth/API/database/environment files changed.

Final report:
Provide:

* Files changed
* Fake/demo content removed
* Layout simplification completed
* What real functionality remains
* Checks run
* Confirmation that no backend, auth, API, upload, brief generation, database, or environment configuration was touched
