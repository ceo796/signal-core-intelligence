Full stop and reconcile the authenticated workspace before any new feature work.

Goal:
Convert the current authenticated Dashboard experience into a clean Signal87 AI Home workspace. The current page still feels too congested and still contains dashboard/demo patterns. The Home page should look and behave like the approved reference: sidebar, top search, welcome header, AI command bar, quick actions, unified Recent work, Signal87 AI panel, recent activity, and suggested actions.

Strict scope:
This is a UI/UX cleanup, naming, and layout reconciliation only. Do not change authentication, document upload logic, chat logic, brief generation, APIs, database queries, database schema, environment variables, Clerk/Supabase/OpenAI configuration, landing page CSS, or protected platform functionality.

Preserve recent completed work:

* Preserve the warm cream / ink design token system in `src/index.css`.
* Preserve the scoped landing page CSS.
* Preserve the new file-type utility.
* Preserve sidebar token styling and left-rail active state.
* Preserve Documents search behavior.
* Preserve Upgrade page layout integration.
* Do not reintroduce violet as the primary design color.

Core product decision:
Remove the user-facing concept of “Dashboard.” The authenticated landing page should be called “Home.”

Route handling:

* Keep `/dashboard` working if the app depends on it.
* If safe, `/dashboard` may render the same Home page or redirect to `/home`.
* Do not break existing navigation or protected route behavior.
* Sidebar active item should be “Home.”

Required Home layout:

1. Top search bar
   Placeholder:
   “Search documents, briefs, collections, and more…”

2. Welcome header
   “Welcome back, [user name]”
   Subtext:
   “Your AI workspace for documents, insights, and execution.”

3. Primary AI command bar
   Placeholder:
   “Ask Signal87 anything across your documents…”
   This should be the main intelligence entry point.

4. Quick actions row
   Keep:

   * Upload document
   * Create brief
   * New agent
   * Start workflow
   * New collection

   If any action is not functional, clearly mute or disable it. Do not present placeholders as working features.

5. Main content
   Replace separate “Recent documents” and “Recent briefs” cards with one unified section:
   “Recent work”

   Recent work should combine real documents, briefs, collections, and workflows where available. Use small type labels such as:

   * Document
   * Brief
   * Collection
   * Workflow

   The section should feel like a clean workspace list/table, not a card grid.

6. Signal87 AI panel
   Keep the panel, but remove all fake/demo content.

   Remove:

   * “EXAMPLE ANSWER”
   * Fake AI answer text
   * Fake citations
   * Fake source chips
   * Fake document references
   * Fake suggested follow-up prompts
   * Hardcoded sample research responses

   Empty state should say:
   “Ask Signal87 a question to begin.”

   Subtext:
   “Use general AI, or ground answers in your uploaded documents when sources are available.”

   Keep only real chat/conversation state if it exists.

7. Right column
   Keep compact:

   * Recent activity
   * Suggested actions

   Recent activity should show real activity only. If no real activity exists, show:
   “No recent activity yet.”

   Suggested actions should only show real actions or clearly disabled/planned actions.

Remove entirely:

* User-facing “Dashboard” language
* Separate Recent documents card
* Separate Recent briefs card
* Fake/example AI content
* Fake source chips
* Fake citations
* Hardcoded demo prompts
* Analytics/sparkline/metrics panels
* Busy dashboard-style modules

Design requirements:

* Warm cream background
* Deep ink typography
* Subtle warm borders
* Agentic technical sans-serif
* Minimal shadows
* Fewer panels
* Calm spacing
* Compact right column
* Sidebar active state with muted background and left rail
* No bubbly SaaS styling
* No purple-heavy styling

Mobile requirements:
The Home page should stack cleanly:

1. Search
2. Welcome
3. AI command bar
4. Quick actions
5. Recent work
6. Signal87 AI
7. Recent activity
8. Suggested actions

No horizontal overflow. No cramped card grid.

Implementation approach:
First inspect the current page structure and identify where Dashboard naming, fake AI content, separate recent cards, and demo modules are coming from. Make the smallest safe set of changes to reconcile the page into the Home workspace.

Validation:
Verify:

* Home page renders after login.
* `/dashboard` still works or redirects safely.
* Sidebar Home item is active.
* Upload document entry point still appears and works.
* Create brief entry point still appears and works.
* Search input appears.
* Signal87 AI panel appears with clean empty state.
* No fake AI answer content appears.
* No fake citations appear.
* No fake source chips appear.
* Recent work replaces separate Recent documents and Recent briefs.
* Recent activity is real or shows a quiet empty state.
* Suggested actions are real, disabled, or visually muted.
* Mobile layout stacks cleanly.
* No horizontal overflow.
* No console errors.
* Typecheck passes.

Final report:
Provide:

* Files changed
* Route/page naming changes
* “Dashboard” language removed
* Home layout implemented
* Fake/demo content removed
* Sections merged
* Functionality preserved
* Checks run
* Confirmation that no backend, auth, API, database, upload, brief generation, environment configuration, landing page CSS, or design-token system was changed
