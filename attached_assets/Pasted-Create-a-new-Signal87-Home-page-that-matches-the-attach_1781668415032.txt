Create a new Signal87 Home page that matches the attached reference image exactly in structure, spacing, tone, and behavior.

IMPORTANT SAFETY RULES:
Do not touch authentication, backend APIs, document ingestion, chat endpoints, brief generation logic, database schema, Clerk/Supabase config, or existing working routes.
This is a frontend-only Home page implementation unless existing frontend data hooks already exist.
Do not delete existing pages. Replace only the current Home/Dashboard route UI.
Preserve all existing navigation routes.
Use existing project styling conventions where possible, but this page should visually match the reference.

PAGE GOAL:
Create a clean white enterprise command-center Home page for Signal87 with:
- Left sidebar navigation
- Main “Home / Command Center” content area
- Large ask/search bar
- Action buttons
- Recent documents panel
- Recent briefs panel
- Suggested actions panel
- Top-right notification icon and user avatar

VISUAL STYLE:
- Background: #ffffff or near-white
- Sidebar background: #fbfbfd
- Primary purple: #7c5cff
- Soft purple background: #f3efff
- Text primary: #111827
- Text secondary: #6b7280
- Borders: #e5e7eb
- Border radius: 14px to 18px
- Font: Inter or existing app font
- Minimal shadows, mostly borders
- Lots of clean spacing
- No dark theme on this page
- No oversized cards
- Match the attached screenshot’s proportions closely

FUNCTIONAL REQUIREMENTS:
1. The main ask bar must be a real input, not just a navigation box.
2. Pressing Enter or clicking the arrow should route the user to the existing ask/chat experience with the typed query preserved.
   Example: /ask?q=<encoded query>
   If the current app uses a different ask route, use the existing route.
3. Upload document button should navigate to the existing upload/documents flow.
4. Create brief button should navigate to the existing brief creation page.
5. New agent button should navigate to the existing agents page.
6. Start workflow button should navigate to the existing workflows page.
7. New collection button should navigate to the existing collections page.
8. “View all documents” should navigate to Documents.
9. “View all briefs” should navigate to Briefs.
10. Suggested action cards should navigate to the appropriate existing page with intent/query state where possible:
   - Summarize a collection → collections or ask with action=summarize_collection
   - Compare documents → briefs or ask with action=compare_documents
   - Extract key insights → ask with action=extract_insights
   - Create a brief → briefs

DATA RULE:
Use real data if existing frontend hooks/services are available.
If real data is not easily available, use the sample rows from the screenshot only as UI placeholders, but isolate them in local arrays named SAMPLE_RECENT_DOCUMENTS and SAMPLE_RECENT_BRIEFS so they can be replaced later.

IMPLEMENTATION:
Create or update the Home page component. Use this JSX structure and CSS approach, adapting imports/routes to the existing codebase.

Suggested file:
src/pages/Home.tsx
or update the current dashboard/home component if that is the project convention.

Code:

import { useState } from "react";
import { useNavigate } from "react-router-dom";
import {
  Bell,
  ChevronLeft,
  ArrowRight,
  Home as HomeIcon,
  FileText,
  Folder,
  Layers,
  Bot,
  Workflow,
  Settings,
  UploadCloud,
  Sparkles,
  Box,
  Plus,
  Database,
  FileSpreadsheet,
  File,
  ChevronRight
} from "lucide-react";
import "./Home.css";

const SAMPLE_RECENT_DOCUMENTS = [
  { name: "Q2 Market Research.pdf", collection: "Market Research", updated: "Just now", type: "doc" },
  { name: "Competitive Analysis.docx", collection: "Strategy", updated: "2h ago", type: "word" },
  { name: "Pricing Model.xlsx", collection: "Finance", updated: "Yesterday", type: "excel" },
  { name: "Customer Interviews.pdf", collection: "Customer Insights", updated: "2 days ago", type: "pdf" },
  { name: "Brand Guidelines.pdf", collection: "Brand", updated: "3 days ago", type: "doc" }
];

const SAMPLE_RECENT_BRIEFS = [
  { name: "Go-to-Market Plan", updated: "Updated 2h ago", status: "In Progress" },
  { name: "Competitor Landscape", updated: "Updated yesterday", status: "Review" },
  { name: "Q2 Executive Brief", updated: "Updated 2 days ago", status: "Completed" },
  { name: "Investor Update", updated: "Updated 3 days ago", status: "Draft" },
  { name: "Product Positioning", updated: "Updated 5 days ago", status: "Draft" }
];

export default function Home() {
  const navigate = useNavigate();
  const [query, setQuery] = useState("");

  const submitQuery = () => {
    const trimmed = query.trim();
    if (!trimmed) return;
    navigate(`/ask?q=${encodeURIComponent(trimmed)}`);
  };

  return (
    <div className="s87-shell">
      <aside className="s87-sidebar">
        <div className="s87-brand">
          <div className="s87-logo">87</div>
          <div className="s87-brand-name">Signal87</div>
          <ChevronLeft size={20} className="s87-collapse" />
        </div>

        <nav className="s87-nav">
          <button className="s87-nav-item active" onClick={() => navigate("/")}>
            <HomeIcon size={18} /> Home
          </button>
          <button className="s87-nav-item" onClick={() => navigate("/documents")}>
            <FileText size={18} /> Documents
          </button>
          <button className="s87-nav-item" onClick={() => navigate("/collections")}>
            <Database size={18} /> Collections
          </button>
          <button className="s87-nav-item" onClick={() => navigate("/briefs")}>
            <Layers size={18} /> Briefs
          </button>
          <button className="s87-nav-item" onClick={() => navigate("/agents")}>
            <Bot size={18} /> Agents
          </button>
          <button className="s87-nav-item" onClick={() => navigate("/workflows")}>
            <Workflow size={18} /> Workflows
          </button>
          <button className="s87-nav-item" onClick={() => navigate("/settings")}>
            <Settings size={18} /> Settings
          </button>
        </nav>

        <div className="s87-account">
          <div className="s87-account-avatar">AC</div>
          <div>
            <div className="s87-account-name">Acme Corp</div>
            <div className="s87-account-user">Emily Chen</div>
          </div>
          <ChevronRight size={16} />
        </div>
      </aside>

      <main className="s87-main">
        <header className="s87-topbar">
          <div>
            <h1>Home</h1>
            <p>Command Center</p>
          </div>
          <div className="s87-top-actions">
            <Bell size={21} />
            <div className="s87-user-avatar">EC</div>
          </div>
        </header>

        <section className="s87-ask">
          <div className="s87-ask-icon">
            <Sparkles size={22} />
          </div>
          <input
            value={query}
            onChange={(event) => setQuery(event.target.value)}
            onKeyDown={(event) => {
              if (event.key === "Enter") submitQuery();
            }}
            placeholder="Ask Signal87 across your documents"
          />
          <button onClick={submitQuery} aria-label="Ask Signal87">
            <ArrowRight size={21} />
          </button>
        </section>

        <section className="s87-actions">
          <button onClick={() => navigate("/documents")}>
            <UploadCloud size={17} /> Upload document
          </button>
          <button onClick={() => navigate("/briefs")}>
            <FileText size={17} /> Create brief
          </button>
          <button onClick={() => navigate("/agents")}>
            <Box size={17} /> New agent
          </button>
          <button onClick={() => navigate("/workflows")}>
            <Sparkles size={17} /> Start workflow
          </button>
          <button onClick={() => navigate("/collections")}>
            <Folder size={17} /> New collection
          </button>
        </section>

        <section className="s87-grid">
          <div className="s87-card s87-documents-card">
            <div className="s87-card-header">
              <h2>Recent documents</h2>
              <button onClick={() => navigate("/documents")}>View all</button>
            </div>

            <div className="s87-table">
              <div className="s87-table-head">
                <span>Name</span>
                <span>Collection</span>
                <span>Last updated</span>
              </div>

              {SAMPLE_RECENT_DOCUMENTS.map((doc) => (
                <div className="s87-table-row" key={doc.name}>
                  <span className="s87-doc-name">
                    <span className={`s87-file-icon ${doc.type}`}>
                      {doc.type === "excel" ? <FileSpreadsheet size={15} /> : <File size={15} />}
                    </span>
                    {doc.name}
                  </span>
                  <span>{doc.collection}</span>
                  <span>{doc.updated}</span>
                </div>
              ))}
            </div>

            <button className="s87-link" onClick={() => navigate("/documents")}>
              View all documents <ArrowRight size={15} />
            </button>
          </div>

          <div className="s87-card">
            <div className="s87-card-header">
              <h2>Recent briefs</h2>
              <button onClick={() => navigate("/briefs")}>View all</button>
            </div>

            <div className="s87-brief-list">
              {SAMPLE_RECENT_BRIEFS.map((brief) => (
                <div className="s87-brief-row" key={brief.name}>
                  <FileText size={21} />
                  <div>
                    <strong>{brief.name}</strong>
                    <span>{brief.updated}</span>
                  </div>
                  <em className={`s87-status ${brief.status.toLowerCase().replaceAll(" ", "-")}`}>
                    {brief.status}
                  </em>
                </div>
              ))}
            </div>

            <button className="s87-link" onClick={() => navigate("/briefs")}>
              View all briefs <ArrowRight size={15} />
            </button>
          </div>
        </section>

        <section className="s87-card s87-suggested">
          <h2>Suggested actions</h2>

          <div className="s87-suggested-grid">
            <button onClick={() => navigate("/collections")}>
              <span><FileText size={17} /></span>
              <div>
                <strong>Summarize a collection</strong>
                <p>Get a summary of key insights</p>
              </div>
              <ChevronRight size={17} />
            </button>

            <button onClick={() => navigate("/briefs?action=compare")}>
              <span><FileText size={17} /></span>
              <div>
                <strong>Compare documents</strong>
                <p>Find similarities and differences</p>
              </div>
              <ChevronRight size={17} />
            </button>

            <button onClick={() => navigate("/ask?action=extract_insights")}>
              <span><FileText size={17} /></span>
              <div>
                <strong>Extract key insights</strong>
                <p>Identify themes and takeaways</p>
              </div>
              <ChevronRight size={17} />
            </button>

            <button onClick={() => navigate("/briefs")}>
              <span><FileText size={17} /></span>
              <div>
                <strong>Create a brief</strong>
                <p>Generate a draft brief instantly</p>
              </div>
              <ChevronRight size={17} />
            </button>
          </div>
        </section>
      </main>
    </div>
  );
}

Now create this CSS file beside the component:

src/pages/Home.css

Code:

.s87-shell {
  min-height: 100vh;
  display: grid;
  grid-template-columns: 280px 1fr;
  background: #ffffff;
  color: #111827;
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

.s87-sidebar {
  border-right: 1px solid #e5e7eb;
  background: #fbfbfd;
  padding: 24px 16px;
  display: flex;
  flex-direction: column;
  gap: 28px;
}

.s87-brand {
  height: 44px;
  display: flex;
  align-items: center;
  gap: 12px;
}

.s87-logo {
  width: 32px;
  height: 32px;
  border: 2px solid #7c5cff;
  border-radius: 999px;
  color: #7c5cff;
  display: grid;
  place-items: center;
  font-weight: 700;
  font-size: 13px;
}

.s87-brand-name {
  font-size: 21px;
  font-weight: 750;
  letter-spacing: -0.04em;
}

.s87-collapse {
  margin-left: auto;
  color: #111827;
}

.s87-nav {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.s87-nav-item {
  width: 100%;
  height: 44px;
  border: 0;
  border-radius: 8px;
  background: transparent;
  color: #4b5563;
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 0 16px;
  font-size: 14px;
  font-weight: 550;
  cursor: pointer;
  text-align: left;
}

.s87-nav-item:hover,
.s87-nav-item.active {
  background: #f3efff;
  color: #7c5cff;
}

.s87-account {
  margin-top: auto;
  height: 70px;
  border: 1px solid #e5e7eb;
  border-radius: 14px;
  background: #ffffff;
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px;
}

.s87-account-avatar {
  width: 40px;
  height: 40px;
  border-radius: 12px;
  background: #7c5cff;
  color: #ffffff;
  display: grid;
  place-items: center;
  font-weight: 700;
}

.s87-account-name {
  font-size: 14px;
  font-weight: 700;
}

.s87-account-user {
  font-size: 13px;
  color: #6b7280;
}

.s87-main {
  padding: 72px 64px 48px;
}

.s87-topbar {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 30px;
}

.s87-topbar h1 {
  margin: 0;
  font-size: 30px;
  line-height: 1.05;
  letter-spacing: -0.05em;
  font-weight: 760;
}

.s87-topbar p {
  margin: 10px 0 0;
  color: #6b7280;
  font-size: 15px;
}

.s87-top-actions {
  display: flex;
  align-items: center;
  gap: 22px;
  color: #374151;
}

.s87-user-avatar {
  width: 40px;
  height: 40px;
  border: 1px solid #e5e7eb;
  border-radius: 999px;
  display: grid;
  place-items: center;
  font-size: 13px;
  font-weight: 600;
  color: #4b5563;
  background: #ffffff;
}

.s87-ask {
  height: 90px;
  border: 1px solid #d9dce3;
  border-radius: 13px;
  display: flex;
  align-items: center;
  gap: 22px;
  padding: 0 22px;
  margin-bottom: 48px;
  background: #ffffff;
}

.s87-ask-icon {
  width: 42px;
  height: 42px;
  border-radius: 10px;
  display: grid;
  place-items: center;
  color: #7c5cff;
  background: #f3efff;
  border: 1px solid #e7ddff;
}

.s87-ask input {
  flex: 1;
  height: 100%;
  border: 0;
  outline: 0;
  background: transparent;
  font-size: 15px;
  color: #111827;
}

.s87-ask input::placeholder {
  color: #7b8190;
}

.s87-ask button {
  width: 38px;
  height: 38px;
  border: 0;
  border-radius: 999px;
  background: #f3efff;
  color: #7c5cff;
  display: grid;
  place-items: center;
  cursor: pointer;
}

.s87-actions {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 24px;
  margin-bottom: 48px;
}

.s87-actions button {
  height: 48px;
  border-radius: 8px;
  border: 1px solid #e1e4ea;
  background: #ffffff;
  color: #111827;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  font-size: 13px;
  font-weight: 650;
  cursor: pointer;
}

.s87-actions button:hover {
  border-color: #cfc7ff;
  background: #fbfaff;
}

.s87-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24px;
  margin-bottom: 32px;
}

.s87-card {
  border: 1px solid #e1e4ea;
  border-radius: 13px;
  background: #ffffff;
  padding: 28px;
}

.s87-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 26px;
}

.s87-card h2,
.s87-card-header h2 {
  margin: 0;
  font-size: 18px;
  letter-spacing: -0.035em;
  font-weight: 760;
}

.s87-card-header button {
  border: 0;
  background: transparent;
  color: #6b7280;
  font-size: 13px;
  cursor: pointer;
}

.s87-table {
  width: 100%;
}

.s87-table-head,
.s87-table-row {
  display: grid;
  grid-template-columns: 1.4fr 1fr 0.7fr;
  align-items: center;
  min-height: 54px;
  border-bottom: 1px solid #edf0f4;
  gap: 14px;
}

.s87-table-head {
  min-height: 38px;
  color: #7b8190;
  font-size: 12px;
}

.s87-table-row {
  color: #4b5563;
  font-size: 13px;
}

.s87-doc-name {
  display: flex;
  align-items: center;
  gap: 12px;
}

.s87-file-icon {
  width: 22px;
  height: 22px;
  border-radius: 4px;
  display: grid;
  place-items: center;
  color: #ffffff;
}

.s87-file-icon.doc {
  background: #7c5cff;
}

.s87-file-icon.word {
  background: #3b82f6;
}

.s87-file-icon.excel {
  background: #58b957;
}

.s87-file-icon.pdf {
  background: #ef4444;
}

.s87-brief-list {
  display: flex;
  flex-direction: column;
}

.s87-brief-row {
  min-height: 62px;
  border-bottom: 1px solid #edf0f4;
  display: grid;
  grid-template-columns: 28px 1fr auto;
  align-items: center;
  gap: 12px;
  color: #4b5563;
}

.s87-brief-row strong {
  display: block;
  font-size: 14px;
  color: #111827;
  margin-bottom: 3px;
}

.s87-brief-row span {
  display: block;
  font-size: 13px;
  color: #6b7280;
}

.s87-status {
  font-style: normal;
  font-size: 12px;
  font-weight: 650;
  border-radius: 6px;
  padding: 6px 10px;
  background: #f3f4f6;
  color: #6b7280;
}

.s87-status.in-progress {
  background: #f3efff;
  color: #7c5cff;
}

.s87-status.review {
  background: #e8f1ff;
  color: #3b82f6;
}

.s87-status.completed {
  background: #e8f7ec;
  color: #52a35e;
}

.s87-link {
  margin-top: 22px;
  border: 0;
  background: transparent;
  color: #7c5cff;
  font-size: 14px;
  font-weight: 600;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  cursor: pointer;
}

.s87-suggested {
  padding: 28px;
}

.s87-suggested h2 {
  margin-bottom: 26px;
}

.s87-suggested-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 18px;
}

.s87-suggested-grid button {
  min-height: 84px;
  border: 1px solid #e1e4ea;
  border-radius: 8px;
  background: #ffffff;
  display: grid;
  grid-template-columns: 28px 1fr 16px;
  align-items: center;
  gap: 12px;
  padding: 16px;
  text-align: left;
  cursor: pointer;
}

.s87-suggested-grid button:hover {
  border-color: #cfc7ff;
  background: #fbfaff;
}

.s87-suggested-grid button span {
  color: #7c5cff;
}

.s87-suggested-grid strong {
  display: block;
  font-size: 13px;
  color: #111827;
  margin-bottom: 8px;
}

.s87-suggested-grid p {
  margin: 0;
  color: #6b7280;
  font-size: 13px;
}

@media (max-width: 1100px) {
  .s87-shell {
    grid-template-columns: 1fr;
  }

  .s87-sidebar {
    display: none;
  }

  .s87-main {
    padding: 32px 20px;
  }

  .s87-actions,
  .s87-grid,
  .s87-suggested-grid {
    grid-template-columns: 1fr;
  }

  .s87-ask {
    height: 74px;
    margin-bottom: 24px;
  }
}

After implementation:
- Verify the route loads without console errors.
- Verify the ask input preserves the query into /ask?q=...
- Verify all buttons navigate correctly.
- Do not mark complete until the page visually matches the reference screenshot closely.