Convert the authenticated “Dashboard” page into the Signal87 AI “Home” page.

Core decision:
I no longer want a page that feels like a dashboard. The authenticated landing experience after login should be a clean Home workspace like the attached reference: sidebar navigation, top search, welcome header, AI command bar, quick actions, workspace content, recent activity, and suggested actions.

Strict scope:
This is a UI/UX and naming/layout refinement only. Do not change authentication, document upload logic, chat logic, brief generation, APIs, database queries, database schema, environment variables, Clerk/Supabase/OpenAI configuration, or protected platform functionality.

Important:
Keep the existing route working if other parts of the app depend on `/dashboard`, but remove user-facing “Dashboard” language. If needed, `/dashboard` can continue to render the Home page or redirect to `/home`. Do not break existing navigation.

Desired user-facing page:

* Sidebar active item should be “Home.”
* The page should feel like the product’s command workspace, not an analytics dashboard.
* Use the current warm cream / ink design system.
* Preserve the recent token work in `src/index.css`.
* Do not reintroduce violet-heavy styling.
* Do not alter the landing page CSS.

Home page structure:

1. Top global search bar:

   * Placeholder: “Search documents, briefs, collections, and more…”
   * Keep visually calm and aligned with the warm cream/ink system.

2. Welcome header:

   * “Welcome back, [user name]”
   * Subtext: “Your AI workspace for documents, insights, and execution.”

3. Primary AI command bar:

   * Placeholder: “Ask Signal87 anything across your documents…”
   * This should become the main Home page intelligence entry point.
   * It should support the hybrid answering behavior when implemented.

4. Quick actions row:

   * Upload document
   * Create brief
   * New agent
   * Start workflow
   * New collection
     Only keep these if the actions are real or already planned. If an action is not functional, visually mute it or disable it clearly.

5. Main workspace content:

   * Use a clean “Recent work” section rather than a cluttered analytics dashboard.
   * If current documents and briefs are separate, merge them into one unified “Recent work” list unless the current code strongly depends on separation.
   * Recent work should include mixed items with type labels such as Document, Brief, Collection, or Workflow.
   * Do not show fake documents, fake briefs, fake citations, or hardcoded demo content.

6. Signal87 AI panel:

   * Keep this as the core agent panel.
   * Do not show fake/example answers.
   * Empty state should say:
     “Ask Signal87 a question to begin.”
     Subtext:
     “Use general AI, or ground answers in your uploaded documents when sources are available.”
   * Keep only real chat state or a clean empty state.
   * No fake answer text.
   * No fake source chips.
   * No fake suggested follow-up prompts.

7. Right column:

   * Recent activity
   * Suggested actions
     Keep these compact and secondary.
     Recent activity should show real activity only. If there is no real activity, use:
     “No recent activity yet.”
     Suggested actions should only include real actions or clearly disabled/planned actions.

Remove:

* User-facing “Dashboard” naming
* Fake/example AI answers
* Fake citations
* Fake source chips
* Fake sample documents/briefs if they are not real
* Analytics/sparkline/metrics panels
* Busy dashboard-style modules
* Purple-heavy active states

Design style:

* Warm cream background
* Deep ink typography
* Subtle warm borders
* Agentic technical sans-serif
* Minimal shadows
* Fewer panels
* Calm spacing
* Sidebar active state with thin rail or muted background
* No bubbly SaaS styling

Mobile:
The Home page should stack cleanly:

1. Search
2. Welcome
3. AI command bar
4. Quick actions
5. Recent work
6. Signal87 AI
7. Recent activity
8. Suggested actions

Validation:
Verify:

* Home page renders after login
* `/dashboard` still works or redirects safely
* Sidebar Home item is active
* Upload document entry point still works
* Create brief entry point still works
* Search input still appears
* Signal87 AI panel still appears
* No fake AI answer content appears
* No fake citations appear
* No backend/auth/API/database/environment files changed
* Mobile has no horizontal overflow
* Typecheck passes

Final report:
Provide:

* Files changed
* Route/page naming changes
* What “Dashboard” language was removed
* What Home layout was implemented
* What fake/demo content was removed
* What functionality was preserved
* Checks run
* Confirmation that no backend, auth, API, database, upload, brief generation, or environment configuration was changed
