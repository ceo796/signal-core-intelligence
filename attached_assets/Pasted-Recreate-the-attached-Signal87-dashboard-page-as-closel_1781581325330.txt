Recreate the attached Signal87 dashboard page as closely as possible.

Goal:
Implement a polished authenticated dashboard/home page matching the attached screenshot. This should become the main signed-in landing page after login.

Critical rules:

* Frontend UI only.
* Do not change backend auth.
* Do not change Clerk configuration.
* Do not change API routes.
* Do not change database schema.
* Do not change owner_user_id isolation.
* Do not change upload logic.
* Do not change extraction/chunking.
* Do not change chat retrieval.
* Do not change citations.
* Do not change brief generation.
* Do not change model routing.
* Preserve authenticated API token handling.
* Preserve public user isolation.

Design target:
Match the attached dashboard screenshot exactly in structure, spacing, typography, and visual style.

Layout:

* Light background, near-white canvas.
* Fixed left sidebar.
* Main content area with max-width spacing similar to screenshot.
* Top horizontal search bar.
* Top-right notification icon and user avatar/name dropdown.
* Rounded cards with subtle gray borders.
* Soft purple active states and action accents.
* Clean Inter-style typography.

Sidebar:

* Signal87 logo at top.
* Navigation items:

  * Home
  * Documents
  * Collections
  * Briefs
  * Agents
  * Workflows
  * Settings
* Active Home item should have pale purple background and purple icon/text.
* Bottom account/workspace card showing current user/workspace.
* Use real authenticated user name/email where available.
* Do not hardcode “Emily Chen” or “Acme Corp” unless only used as placeholder in a static mock state.

Top bar:

* Global search input:
  “Search documents, briefs, collections, and more...”
* Keyboard hint: ⌘K
* Notification icon.
* User avatar/name dropdown using authenticated user data.

Main header:

* “Welcome back, [First Name] 👋”
* Subtitle:
  “Your AI workspace for documents, insights, and execution.”
* Use the logged-in user’s first name if available.
* Fallback: “Welcome back 👋”

Primary AI command bar:

* Large rounded input card.
* Left sparkle/AI icon.
* Placeholder:
  “Ask Signal87 anything across your documents”
* Purple circular arrow/send button on right.
* If global cross-document chat is already implemented, wire it safely through the authenticated existing API.
* If not fully implemented, keep it as a UI entry point but do not call broken backend logic.

Action row:
Create five horizontally aligned buttons:

* Upload document
* Create brief
* New agent
* Start workflow
* New collection

Behavior:

* Upload document should use the existing working upload flow.
* Create brief should route to the brief builder if /brief is implemented.
* Agents, Workflows, and Collections should not call nonexistent backend logic. If not implemented, mark as disabled or “coming soon” while preserving the visual design.

Recent documents card:

* Use real authenticated user documents from the existing documents API.
* Columns:

  * Name
  * Collection
  * Updated
* Show file-type icons for PDF, DOCX, XLSX, etc.
* If the user has no documents, show a clean empty state:
  “No documents yet. Upload your first document to begin.”
* Do not show “Could not load your documents” unless there is a real network/server/auth error.
* Do not show legacy ownerless documents.
* Do not show documents belonging to other users.

Recent briefs card:

* Match the attached right-side card design.
* If real briefs are available, show them.
* If briefs are not yet persisted, show an empty state or safe placeholder.
* Do not imply saved brief history exists if it is not implemented.
* Status badges:

  * In Progress
  * Review
  * Completed
  * Draft

Signal87 AI panel:

* Large lower-left card titled:
  “Signal87 AI”
* Add small “Beta” badge.
* Subtitle:
  “I have access to your documents, briefs, and collections.”
* If collections/briefs are not fully wired, adjust copy to:
  “I can answer questions across your uploaded documents.”
* Show a clean chat-style interface:

  * user message bubble
  * AI answer card
  * citation chips
  * suggested follow-up chips
  * follow-up input box
* Use real chat/citation behavior only if safely wired.
* If not wired globally, this panel should route users into existing document chat rather than calling broken logic.
* Do not invent citations from fake documents in production user accounts.

Recent activity card:

* Right-side card.
* If real activity tracking exists, use it.
* If not, show a clean empty state:
  “No recent activity yet.”
* Do not hardcode fake activity for real users.

Suggested actions card:

* Right-side lower card.
* Actions:

  * Summarize a collection
  * Compare documents
  * Extract key insights
  * Create a brief
* Only enable actions that are backed by working routes.
* Disable or mark future actions as “Coming soon” if not implemented.

Typography:

* Use Inter as the primary font if available.
* Headings: 600–700 weight.
* Body: 400–500.
* Muted labels: soft gray.
* Match the calm, spacious font scale from the screenshot.

Visual style:

* Background: #FAFAFB or similar.
* Cards: white.
* Border: subtle #E5E7EB / #ECECF0.
* Accent: soft purple/lavender.
* Active nav: pale purple.
* Primary icon/button accent: purple.
* Corners: 12–18px.
* Shadows: very subtle or none.
* Keep the interface airy and premium.

Responsive behavior:

* Desktop should closely match the screenshot.
* Tablet should preserve sidebar where possible.
* Mobile should collapse sidebar into drawer or bottom navigation.
* Cards should stack cleanly.
* No horizontal overflow.
* All buttons must remain tappable.

Required QA:

* Typecheck passes.
* Build passes.
* Logged-out /api/documents returns 401.
* Logged-in dashboard loads.
* New user with no documents sees a clean empty state.
* Upload document still works.
* Uploaded document appears in Recent documents.
* Document detail still opens.
* Chat still works where already implemented.
* Citations still render.
* User A cannot see User B’s documents.
* No auth/API/backend/security files were changed unless explicitly necessary for route registration.
* No fake demo documents appear in real public user accounts.

Report back with:

* files changed
* dashboard route updated
* data sources used
* disabled/future elements
* font used
* responsive behavior
* QA results
* confirmation that auth, owner_user_id isolation, upload, chat, citations, and model routing were preserved
