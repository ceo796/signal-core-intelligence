I uploaded a standalone file named signal87-landing.html. Safely integrate it into this existing Signal87 AI Replit project as a new landing page without breaking the current authenticated application.

Critical safety rules:

1. Create a checkpoint before making changes.
2. Do not modify backend routes, API logic, auth logic, document upload, document indexing, chat, database schema, Clerk/Supabase configuration, or protected app routes.
3. Do not paste the raw standalone HTML directly into App.tsx or index.html.
4. Convert the standalone HTML into a proper React/Vite page.

Implementation requirements:

1. Create a new route first at /landing.
2. Convert the HTML body into a React component:
    * client/src/pages/LandingPage.tsx or the closest existing page structure.
3. Move the CSS into a scoped stylesheet:
    * client/src/styles/landing.css or equivalent.
4. Prefix/scope the landing styles under a wrapper class like .signal87-landing so the landing page CSS cannot affect the existing product app.
5. Import the Google fonts in the safest existing frontend location, or use the current app font system if already configured.
6. Convert the JavaScript canvas animations into React useEffect hooks with proper cleanup:
    * cancel requestAnimationFrame
    * remove resize listeners
    * remove scroll listeners
    * prevent duplicate animation loops during hot reload
7. Keep only one hero signal animation:
    * one single animated waveform / pulse line
    * slow horizontal trace
    * clean, abstract, technical
    * purple/lavender glow
    * no busy multi-line signal background
8. Keep the page visually close to the supplied landing file, but make the hero feel more like Signal87’s current clean enterprise UI style: minimal, premium, technical, restrained.
9. Wire links safely:
    * “Sign in” should go to the existing sign-in/auth route.
    * “Start free trial,” “Request access,” and “Talk to sales” should go to existing signup/contact routes if present; otherwise use safe placeholders and clearly report that.
10. Remove or flag unverified claims before production, especially:

* “SOC 2 Type II certified”
* “classified-ready infrastructure”
* any government/defense claim that is not already approved marketing copy.

11. After integration, run:

* typecheck
* build
* local preview

12. Confirm:

* /landing loads correctly
* existing app routes still load
* auth is untouched
* no backend/API files were changed

13. Report exactly which files were created or modified.

Do not make / point to this page yet. First complete /landing, verify it, and then ask before replacing the homepage.