We are preparing Signal87 AI for deployment tonight. Do not work on app.signal87.ai or PWA setup right now.

Priority: implement a safe, simple user account access process without disrupting the existing platform.

Critical rules:

* Do not change backend document logic.
* Do not change upload, extraction, chat, citations, brief generation, model routing, database document structure, or existing working API paths.
* Do not introduce a full billing system tonight.
* Do not redesign the app.
* This is an authentication and access-control readiness pass only.

Goal:
Make the platform safe to deploy by ensuring only authenticated and approved users can access the real app.

Please do the following:

1. Audit current auth flow

* Confirm how login currently works.
* Confirm whether Google sign-in is working.
* Confirm which routes are protected and which are public.
* Identify any app pages that can be accessed without authentication.

2. Protect private app routes
    Require authentication for all core app pages, including:

* dashboard
* documents
* document detail
* chat/intelligence
* brief generation
* settings/account
* any workspace, upload, or analysis pages

Public pages may remain public:

* landing page
* login/signup page
* terms/privacy pages if present

3. Add approved-user gate
    Create or use a simple approved-user mechanism so that only approved emails can access the full app.

Preferred approach:

* Check the logged-in user’s email after authentication.
* If the email is approved, allow access to the app.
* If not approved, route the user to a clean “pending access” screen.

The pending access screen should say something like:
“Your account has been created. Access to Signal87 AI is currently approval-based. Please contact the Signal87 team to activate your account.”

4. Keep account process simple

* Do not add complex onboarding.
* Do not add Stripe.
* Do not add teams/workspaces unless already implemented.
* Do not add invite flows unless simple and low-risk.
* Do not add password reset complexity if Google OAuth is already the main path.

5. Add basic account safety checks

* Make sure each authenticated user only sees their own documents/data unless existing admin functionality intentionally allows otherwise.
* Confirm Supabase RLS or equivalent access controls are still active.
* Confirm no documents leak across users.
* Confirm logged-out users are redirected safely.

6. Deployment readiness QA
    After changes, test:

* logged-out user cannot access dashboard/documents/chat
* approved user can log in and access the app
* unapproved user logs in but only sees pending access page
* approved user can upload a document
* approved user can open document detail
* approved user can ask chat questions
* citations still render
* brief generation still works if already working
* desktop layout still works
* mobile layout still works

7. Report back
    Summarize:

* files changed
* auth behavior before and after
* how approved users are managed
* what was not touched
* remaining deployment risks

Again: do not refactor core intelligence logic. The objective is to make tonight’s deployment safe, gated, and stable.