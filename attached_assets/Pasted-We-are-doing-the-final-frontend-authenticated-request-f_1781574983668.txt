We are doing the final frontend authenticated-request fix before UI polish.

Do not start UI redesign yet. Do not change backend auth, Clerk architecture, database schema, owner_user_id isolation, upload/extraction logic, chat retrieval, brief generation, citations, prompts, or model routing.

Goal:
Fix the “Could not load your documents” / upload auth gap by ensuring every protected frontend API request sends the Clerk auth token.

Tasks:

1. Fix upload auth

* In file-upload.tsx, replace the raw fetch("/api/documents/upload", ...) with an authenticated request.
* Preferred: use the existing authenticated/custom fetch helper if it supports FormData.
* If not, use Clerk useAuth().getToken() and attach:
  Authorization: Bearer <token>
* Do not manually set Content-Type for FormData.

2. Audit all frontend API calls
   Search for:

* fetch("/api/
* fetch(`/api/
* axios calls, if any

Any protected API route must use authenticated fetch or attach the Clerk bearer token.

Protected routes include:

* /api/documents
* /api/documents/upload
* /api/documents/:id
* /api/documents/:id/chunks
* /api/documents/:id/original
* /api/documents/:id/chat
* /api/documents/:id/history
* /api/documents/:id/reindex
* /api/documents/multi-chat
* /api/documents/brief
* delete/download/reindex routes

Public routes like /api/healthz may remain unauthenticated.

3. Fix empty state
   If logged-in /api/documents returns 200 with an empty list, the dashboard should show:
   “No documents yet. Upload your first document to begin.”

Do not show “Could not load your documents” unless there is a real network/server/auth error.

4. Run QA
   Verify:

* typecheck passes
* logged-out /api/documents returns 401
* logged-in /documents loads
* new signed-in user with no documents sees empty state
* upload succeeds
* uploaded document gets owner_user_id
* uploaded document appears after refresh
* another user cannot see it
* chat works
* citations render
* brief backend remains protected

5. Republish
   After the fix passes locally/dev, republish the app.

6. Production verification
   On the live URL, confirm:

* logged-out /api/documents returns 401
* logged-in /documents loads
* upload works
* uploaded document appears
* User A/User B isolation still passes

7. Report back
   Include:

* raw fetch calls found
* files changed
* which calls were fixed
* which calls were intentionally left public
* dev QA result
* production QA result
* whether the build is now ready for UI polish

After this is complete, we will move to UI work only: landing page, document dashboard, empty states, and mobile polish. UI work must not touch auth, API routes, owner_user_id, upload logic, chat, briefs, citations, or model routing.
