{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "verify:theme": "node scripts/verify-signal87-theme.mjs",
    "build": "pnpm run typecheck && pnpm run verify:theme && pnpm -r --if-present run build",
    "build:production": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/signal87-core run build",
    "start": "node --enable-source-maps ./production-server.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "db:push": "pnpm --filter @workspace/db run push",
    "test": "pnpm --filter @workspace/api-server run test",
    "smoke:api": "node scripts/smoke-test-api.mjs",
    "smoke:audit": "bash scripts/audit-api.sh",
    "smoke:auth": "node scripts/smoke-auth-checklist.mjs",
    "smoke:ui": "node scripts/ui-audit.mjs",
    "smoke:production": "node scripts/smoke-test-production.mjs",
    "test:billing": "node scripts/test-stripe-billing.mjs",
    "test:signup-stripe": "node scripts/test-signup-stripe-e2e.mjs",
    "generate:opengraph": "node scripts/generate-opengraph.mjs"
  },
  "private": true,
  "devDependencies": {
    "playwright": "1.52.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
